export enum KeyMessage {
    Succes = 'succes',
    Error = 'error'
}

export enum PrefixMessage {
    Deleted = 'deleted',
    Added = 'added',
    Saved = 'saved'
}

export enum ActionPersons {
    Deleted = 'deletePerson',
    Added = 'addPerson',
    Saved = 'savePerson'
}